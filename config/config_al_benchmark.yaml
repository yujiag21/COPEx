
defaults:
  - optimizer: slsqp 
  - warmstart: default 
  - experiment: al_benchmark
  - task: al_benchmark
  - paths: al_benchmark
  - embedder: embedder
  - encoder: encoder
  - head: head
  - _self_

acquisition_function: acquisition_functions.eig_acquisition.EntropyDifferenceEIG

task:
  dim_x: 2
  benchmark_name: ackley2d 
experiment:
  trial: 17
  verbose: 5
  budget: 50.0
  n_init_evals: 1
#  dim: 2
  step_size: 1
  n_step: 51
  n_target_theta: 0
  lookahead_n_fantasies: 2
  n_fantasies: 3
  n_y: 20
  discount_factor: 0.8
  fantasized_with_model: false
  cost_mode: quadratic 
  constraint_mode: linf 

warmstart:
  n_random: 4
  n_optimal: 1
  top_k_starts: 5

paths:
  model_path: ALINE/models/AL/inference/inference_model.pth
  design_model_path: ALINE/models/AL/design/design_model.pth
  config_name: train_prediction_design_al_benchmark
  design_config_name: train
  save_path: AL_terrain_function
  save_folder: AL_terrain_function

optimizer:
  lower_bound: -5.0
  upper_bound: 5.0
  optimize_mode: soft 
  soft_constraint_weight: 1 

hydra:
  run:
    dir: outputs/${now:%Y-%m-%d}/${now:%H-%M-%S}
  sweep:
    dir: outputs/multirun/${now:%Y-%m-%d}/${now:%H-%M-%S}
    subdir: ${hydra.job.num}
  job:
    chdir: false

device: "cuda"
fix_seed: true
seed: 42
time_token: false
